# Images

- `profile.jpg` — headshot used on the home page and as the social preview image
  (Open Graph / Twitter card). Roughly square, ideally 800×800 or larger.

To change the social preview image site-wide, edit `website: image:` in `_quarto.yml`.
